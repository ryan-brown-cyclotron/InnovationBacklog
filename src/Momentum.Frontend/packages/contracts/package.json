{
  "name": "@momentum/contracts",
  "version": "1.0.0",
  "description": "TypeScript mirrors of the Momentum.Contracts C# shared models (auto-generated by TypeGen)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "generate": "node scripts/generate.cjs",
    "build": "pnpm generate && tsc -b --force",
    "clean": "node -e \"require('node:fs').rmSync('src/generated', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
